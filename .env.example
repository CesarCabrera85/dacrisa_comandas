# DACRISA Comandas - Environment Variables Example
# Copy this file to .env and fill in the values

# ===========================================
# DOMAIN & HTTPS CONFIGURATION
# ===========================================
# For local development: use localhost
# For production: use your actual domain (e.g., comandas.example.com)
DOMAIN=localhost

# Email for Let's Encrypt certificate notifications (required for production)
ACME_EMAIL=admin@example.com

# ===========================================
# BASE URLs
# ===========================================
# Base URL for the API (used for generating PDF URLs, etc.)
BASE_URL=https://localhost
VITE_API_URL=/api

# ===========================================
# DATABASE
# ===========================================
# PostgreSQL password (change in production!)
POSTGRES_PASSWORD=postgres

# Full database URL (usually auto-generated by docker-compose)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/dacrisa_comandas

# ===========================================
# SECURITY - CHANGE ALL IN PRODUCTION!
# ===========================================
# 64-character hex string for code encryption (AES-256)
# Generate with: openssl rand -hex 32
CODE_ENC_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Session secret (at least 32 characters)
# Generate with: openssl rand -hex 16
SESSION_SECRET=supersecretkey123456789012345678

# HMAC secret for code lookup (at least 32 characters)
# Generate with: openssl rand -hex 16
CODE_LOOKUP_SECRET=lookupsecret123456789012345678901

# ===========================================
# IMAP EMAIL CONFIGURATION
# ===========================================
# IMAP server settings for email ingestion
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=pedidos@example.com
IMAP_PASSWORD=your_app_password_here
IMAP_FOLDER=INBOX
IMAP_POLL_SECONDS=15
IMAP_SECURE=true

# ===========================================
# FUZZY MATCHING
# ===========================================
# Threshold for product name matching (0-100)
FUZZY_MATCH_THRESHOLD=80

# ===========================================
# ENVIRONMENT
# ===========================================
NODE_ENV=production
